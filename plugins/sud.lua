local function run(msg, matches)
      --if is_sudo(msg) then
	if is_momod(msg) then
    end
   end
            return [[ بله مدیر چه کمکی از دستم برمیاد انجام بدم؟ ]]
end return 
{
    patterns = {
    "^بات$"
  },
  run = run
}
 
